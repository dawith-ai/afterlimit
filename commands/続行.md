---
name: 続行
description: 前回の作業を自律的に継続し、再開の安全網を 1 回発火させる(ユーザー向けのメッセージはシンプルに保つ)
---

前回の作業を自律的に継続します。

## ステップ 1 — 安全網を 1 回だけ発火(静かに)

以下を実行しますが、**技術的な出力(スキャン / 発火 / スキップの数値)はユーザーに決して見せないでください**(出力は破棄します):

```bash
__PYTHON__ __REPO_DIR__/scripts/resume_blocked_sessions.py > /dev/null 2>&1
```

## ステップ 2 — 前回の作業を継続

- 確認の質問をせず、未完了の作業をただちに継続します。
- ステップが完了したら 1 行で報告します。

## ユーザー向けメッセージ — シンプルに保つ(重要)

「安全網」「N 件スキャン」「発火」「ブロック済みをスキップ」「HH:MM にリセット」のような技術用語や数値は **一切出さない** でください。
次のトーンで 1 行だけ:

> **「はい、続けます。」**

(役立つ場合は、もう 1 行だけ追加してもよい: 「他のセッションも、上限がリセットされたら自動で継続します。」)

そのあと、すぐに作業を続けます。
